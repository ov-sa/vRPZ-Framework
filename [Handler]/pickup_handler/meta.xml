<meta>

    <info author="OvileAmriam" name="Pickup Handler" type="Handler" version="1.0.0"/>
    <oop>true</oop>

    <!-- Settings -->
    <script src="settings/shared.lua" type="shared" cache="false"/>

    <!-- Handlers -->
    <script src="handlers/server.lua" type="server"/>

    <!-- Params: createPickupItem(pickupDatas, expiryDuration) | Returns Element; Else false Boolean -->
    <export function="createPickupItem" type="server"/>

    <!-- Params: destroyPickupItem(pickup) | Returns Boolean -->
    <export function="destroyPickupItem" type="server"/>

</meta>